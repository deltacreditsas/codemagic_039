name: paydayadvance
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+2

environment:
  sdk: ^3.9.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # 🌍 Localization
  easy_localization: ^3.0.8
  easy_localization_loader: ^2.0.2

  # 🔥 Firebase
  firebase_core: ^4.1.1
  firebase_remote_config: ^6.0.2
  firebase_messaging: ^16.0.2
  firebase_auth: ^6.1.0
  firebase_crashlytics: ^5.0.2

  # Navigation & Routing
  go_router: ^16.2.2

  # Networking / URL
  url_launcher: ^6.3.2
  internet_connection_checker_plus: ^2.7.2
  yaml: ^3.1.3


  # Device & Platform
  wakelock_plus: ^1.4.0
  flutter_udid: ^4.0.0
  shared_preferences: ^2.5.3

  # WebView
  webview_flutter: ^4.13.0
  webview_flutter_android: ^4.10.2

  # Utilities
  logger: ^2.6.1
  get_it: ^8.2.0
  provider: ^6.1.5+1

  # UI & Styling
  flutter_svg: ^2.2.1
  flutter_screenutil: ^5.9.3
  flutter_dash: ^1.0.0
  google_fonts: ^6.3.1
  animated_switcher_plus: ^1.4.0
  flutter_markdown: ^0.7.7+1

  # App Lifecycle & Store
  in_app_review: ^2.0.11
  store_redirect: ^2.0.4
  flutter_native_splash: ^2.4.6


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.4


flutter_icons:
  android: true
  ios: true
  min_sdk_android: 22
  image_path_android: "assets/images/main_logo_play_store.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/images/main_logo_play_store.png"
  image_path_ios: "assets/images/main_logo_app_store.png"
  remove_alpha_ios: true


# The following sections is specific to flutter_native_splash
# Customize the parameters below, and run the following command in the terminal:
# > flutter pub run flutter_native_splash:create
flutter_native_splash:
  color: "#FFFFFF"
  image: "assets/images/native_splash_play_store.png"
  android_12:
    image: "assets/images/native_splash_play_store.png"
    color: "#FFFFFF"
    icon_background_color: "#FFFFFF"
  image_ios: "assets/images/native_splash_app_store.png"
  color_ios: "#FFFFFF"

flutter:


  uses-material-design: true

  assets:
      - assets/images/
      - assets/texts/
      - assets/translations/
